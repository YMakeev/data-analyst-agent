# Оточення
.venv/
venv/
__pycache__/
*.py[cod]

# Креденшели. .env.example — єдиний, який комітиться: у ньому лише
# дефолти одноразового локального контейнера.
.env
.env.*
!.env.example
*.dsn

# Звіти генеруються під час роботи
reports/*.html

# ml/model.pkl НЕ ігнорується навмисно: натренована модель — це частина
# поставки. Її створюють окремою командою make train, перевіряють якість
# і лише потім кладуть у репозиторій. Сервер моделі не навчає.

# Службове
.pytest_cache/
.DS_Store
*.egg-info/
dist/
build/
uv.lock
